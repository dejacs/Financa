<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jadecsilveira.financas">

    <uses-permission android:name="android.permission.SET_DEBUG_APP" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.PaginaInicialActivity"
            android:label="@string/title_activity_pagina_inicial"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.DespesaActivity"
            android:label="@string/title_activity_despesas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.RendimentoActivity"
            android:label="@string/title_activity_rendimentos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.InclusaoDespesaActivity"
            android:label="@string/adicionar" />
        <activity
            android:name=".activity.InclusaoRendimentoActivity"
            android:label="@string/adicionar" />
    </application>

</manifest>
